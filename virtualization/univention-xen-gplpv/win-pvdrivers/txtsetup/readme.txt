Text Mode Setup Install Disk
Copy everything from the drivers directory (*.sys + *.inf + *.dll) into this directory, as well as C:\windows\system32\drivers\*.sys. Probably needs to be a virtual 2880KB disk.
Press F6 at boot time and add all the drivers in order from first to last.
