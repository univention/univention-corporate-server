[Global]
logfile: autotest-update-from-2.4.log

kvm_server: [ENV:KVM_BUILD_SERVER]
kvm_user: [ENV:KVM_USER]
kvm_extra_label: autotest-update-from-2.4-[ENV:UCS_VERSION]

# template was installed with UCS 1.2 and updated
# Others	3.2-7	1.2masterinstallation	i386
# Others	4.3-4	1.2masterinstallation	i386
# Others	4.4-7	2.4masterinstallation	amd64
# Others	5.0-6	2.4masterinstallation	amd64
# Others	5.0-9	2.4masterinstallation	amd64
kvm_operating_system: Others
kvm_template: 2.4masterinstallation
kvm_ucsversion: 5.0-6
kvm_architecture: amd64

kvm_dhcp: true

recover: 4

environment:
 UCS_VERSION=[ENV:UCS_VERSION]
 TARGET_VERSION=[ENV:UCS_VERSION]
 RELEASE_UPDATE=[ENV:RELEASE_UPDATE]
 ERRATA_UPDATE=[ENV:ERRATA_UPDATE]
 JENKINS_WS=[ENV:JOB_URL]

[master]
command1:
 # update to latest released (including errata test)
 . utils.sh && basic_setup
 . utils.sh && add_tech_key_authorized_keys
 . utils.sh; rotate_logfiles || true
 find /var/log/ \( -name \*.gz -o -name '*.[0-9]' -o -name '*.[0-9][0-9]' \) -delete
command2:
 # Check and renew expired SSL certificates if necessary
 . utils.sh && renew_expired_certificates
command3:
 ucr set update/secure_apt=true
 . utils.sh && upgrade_to_latest_test_errata_if
 # upgrade to target version
 . utils.sh && upgrade_to_latest --disable-app-updates --updateto 5.0-9
 . utils.sh && upgrade_to_latest_test_errata_if
command4:
 GET /tmp/ucs.ver ucs_[SELF].ver
 # stop the instance
 . base_appliance.sh && appliance_poweroff
 SSH_DISCONNECT
 SERVER id=$(virsh domid "[SELF_KVM_NAME]") && [ -n "${id#-}" ] && virsh event --domain "$id" --event lifecycle --timeout 120 --timestamp || :
 # remove cdrom
 SERVER virsh detach-disk --config --domain "[SELF_KVM_NAME]" hda
 # create template
 SOURCE ucs_[SELF].ver
 SERVER ucs-kt-put -C single -O Others -c "[SELF_KVM_NAME]" "5.0-9_2.4masterinstallation_amd64"
files:
 ~/ec2/scripts/activate-errata-test-scope.sh /root/
 ~/ec2/license/license.secret /etc/
