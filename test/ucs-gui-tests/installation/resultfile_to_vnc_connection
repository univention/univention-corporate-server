#!/usr/bin/python

import sys
import json


with open(sys.argv[1]) as resultfile:
    result = json.load(resultfile)
    vnc_connection = result[0]['vnchost'] + ':' + str(result[0]['vncport'])
    print vnc_connection
