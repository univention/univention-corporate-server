#!/usr/share/ucs-test/runner python
## desc: Set jpegPhoto during users/user modification
## tags: [udm,apptest]
## roles: [domaincontroller_master]
## exposure: careful
## packages:
##   - univention-config
##   - univention-directory-manager-tools

import base64
import univention.testing.udm as udm_test
import univention.testing.strings as uts
import univention.testing.utils as utils


if __name__ == '__main__':
	with udm_test.UCSTestUDM() as udm:
		mail_domain = '%s.%s' % (uts.random_name(), uts.random_name())
		udm.create_object('mail/domain', name = mail_domain)

		user = udm.create_user(mailPrimaryAddress = '%s@%s' % (uts.random_name(), mail_domain), options = ['mail', 'person'])[0]
	
		with open('/usr/share/ucs-test/61_udm-users/example_user_jpeg_photo.jpg') as jpeg:
			jpeg_data = jpeg.read()

		udm.modify_object('users/user', dn = user, jpegPhoto = base64.b64encode(jpeg_data))
		utils.verify_ldap_object(user, {'jpegPhoto': [jpeg_data]})
