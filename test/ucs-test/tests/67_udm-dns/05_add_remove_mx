#!/usr/share/ucs-test/runner python
## desc: DNS forward zone, test setting, appending and removing MX records
## tags: [udm-dns,apptest]
## roles: [domaincontroller_master]
## bugs: [15654]
## exposure: careful
## packages:
##   - univention-config
##   - univention-directory-manager-tools



import univention.testing.utils as utils
import univention.testing.udm as udm_test
import univention.testing.strings as uts


if __name__ == '__main__':
	MXrecords = ['10 %s' % uts.random_name(), '20 %s' % uts.random_name(), '30 %s' % uts.random_name()]

	with udm_test.UCSTestUDM() as udm:
		forwardZone = udm.create_object('dns/forward_zone', zone = '%s.%s' % (uts.random_name(), uts.random_name()), nameserver = uts.random_string(), mx = MXrecords[0])
		
		udm.modify_object('dns/forward_zone', dn = forwardZone, append = {'mx': MXrecords[1:]})
		if not utils.verify_ldap_object(forwardZone, {'mXRecord': MXrecords}):
			utils.fail()

		udm.modify_object('dns/forward_zone', dn = forwardZone, remove = {'mx': MXrecords[:2]})
		if not utils.verify_ldap_object(forwardZone, {'mXRecord': MXrecords[2:]}):
			utils.fail()
