#!/usr/share/ucs-test/runner python
## desc: Create minimal DHCP pool
## tags: [udm-dhcp,apptest]
## roles: [domaincontroller_master]
## exposure: careful
## packages:
##   - univention-config
##   - univention-directory-manager-tools



import univention.testing.utils as utils
import univention.testing.udm as udm_test
import univention.testing.strings as uts

if __name__ == '__main__':
	with udm_test.UCSTestUDM() as udm:
		utils.verify_ldap_object(udm.create_object('dhcp/pool',
												   range = '10.20.30.1 10.20.30.254',
												   name = uts.random_name(),
												   superordinate = udm.create_object('dhcp/subnet',
																					 subnet = '10.20.30.0',
																					 subnetmask = '24',
																					 superordinate = udm.create_object('dhcp/service', service = uts.random_name())
																					)
							  					  )
								)

