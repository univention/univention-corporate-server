#!/usr/share/ucs-test/runner python
## desc: Create and remove DHCP pool
## tags: [udm-dhcp,apptest]
## roles: [domaincontroller_master]
## exposure: careful
## packages:
##   - univention-config
##   - univention-directory-manager-tools



import univention.testing.utils as utils
import univention.testing.udm as udm_test
import univention.testing.strings as uts

if __name__ == '__main__':
	with udm_test.UCSTestUDM() as udm:
		dhcpService = udm.create_object('dhcp/service', service = uts.random_name())
		dhcpSubnet = udm.create_object('dhcp/subnet', subnet = '10.20.30.0', subnetmask = '24', superordinate = dhcpService)
		dhcpPool = udm.create_object('dhcp/pool', superordinate = dhcpSubnet, name = uts.random_name(), range = '10.20.30.1 10.20.30.254')

		if not utils.verify_ldap_object(dhcpPool):
			utils.fail()

		udm.remove_object('dhcp/pool', dn = dhcpPool, superordinate = dhcpSubnet)
		if utils.verify_ldap_object(dhcpPool):
			utils.fail('Could still find DHCP pool "%s" in LDAP after it should have been removed' % dhcpPool)
