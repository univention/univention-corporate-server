# generate-appliance

Debian source package: Create virtual appliances for various virtualization systems (KVM, VMware, ESX, VirtualBox) from a single disk image.

## Key contents
- `debian/` -- Debian packaging files
- `generate_appliance/` -- Python package with appliance generation logic
- `setup.py`, `setup.cfg` -- Python package configuration
