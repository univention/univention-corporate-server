[Global]
# These settings can be overwritten by the instance
logfile: ~/ec2/logfiles/app-install.log

# AMI: Univention Corporate Server (UCS) 3.1 (official image) rev. 5 (ami-24f0fa50)
ec2_ami: ami-24f0fa50

# EC2 Region
ec2_region: eu-west-1

# In VPC the security group id is required
# #ec2_security_group: UCS systems
ec2_security_group: sg-4170962e

# the ec2 ssh keypair
ec2_keypair: tech
ec2_keypair_file: ~/ec2/keys/tech.pem

# Default intance type
#	t1.micro m1.small m1.large m1.xlarge c1.medium
#	c1.xlarge 	m2.xlarge m2.2xlarge m2.4xlarge cc1.4xlarge
ec2_instance_type: t1.micro

# VPC
ec2_subnet_id: subnet-f452539c

# To get EC2 access you need to create a .boto file
# http://code.google.com/p/boto/wiki/BotoConfig
# [Credentials]
#  aws_access_key_id = <your access key>
#  aws_secret_access_key = <your secret key>

[app-install]
# Maybe there is an old instance which should be reused
# ec2_reuse: i-a6cb12eb
ec2_instance_type: m1.small
profile:
 hostname="ucs"
 domainname="deadlock51.local"
 windows/domain="DEADLOCK51"
 ldap/base="dc=deadlock51,dc=local"
 root_password="univention"
 locale/default="de_DE.UTF-8:UTF-8"
 packages_install="univention-s4-connector univention-samba4"
 packages_remove=""
command1:
 echo -e "#!/bin/sh\nroute del default ; route add default gw 10.210.216.13" >>/etc/network/if-up.d/z_route
 chmod +x /etc/network/if-up.d/z_route
 /etc/network/if-up.d/z_route
 sleep 10 # just wait a few seconds to give the amazone cloud some time
 ucr set updater/identify="UCS (EC2 Test)"
command2:
 univention-upgrade --ignoreterm --ignoressh --noninteractive
command3:
 /usr/lib/univention-system-setup/scripts/setup-join.sh
 /etc/init.d/univention-management-console-server restart
 univention-license-import deadlock51.license
command4:
 univention-add-app --latest -a zarafa
 univention-add-app --latest -a owncloud
 univention-add-app --latest -a drbd
 univention-add-app --latest -a fetchmail
files:
 ~/ec2/scripts/* /usr/sbin
 ~/ec2/license/deadlock51.license /root/
