# univention-directory-policy

Manages the directory policy handler scripts in `/usr/lib/univention-directory-policy`.

## Binary packages

- `univention-directory-policy`

## Directory structure

- `univention-directory-policy-cron` -- Cron-based policy execution script
- `conffiles/` -- UCR templates
- `debian/` -- Debian packaging
