# univention-authorization

Python authorization utilities for UCS, providing authorization logic and policy evaluation.

## Binary packages

- `python3-univention-authorization`

## Directory structure

- `modules/` -- Python modules (`univention.authorization`)
- `debian/` -- Debian packaging
