# univention-management-console-module-top

UMC module for process overview (similar to `top`), using psutil.

## Binary packages

- `univention-management-console-module-top`

## Directory structure

- `umc/` -- UMC module (Python backend + JS frontend)
- `debian/` -- Debian packaging
