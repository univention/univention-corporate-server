# univention-management-console-module-ipchange

UMC module that updates the IP address of a server system in the LDAP directory.

## Binary packages

- `univention-management-console-module-ipchange`

## Directory structure

- `umc/` -- UMC module (Python backend + JS frontend)
- `debian/` -- Debian packaging
