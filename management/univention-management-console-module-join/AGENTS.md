# univention-management-console-module-join

UMC module for domain join actions (running join scripts, re-joining).

## Binary packages

- `univention-management-console-module-join`

## Directory structure

- `umc/` -- UMC module (Python backend + JS frontend)
- `debian/` -- Debian packaging
