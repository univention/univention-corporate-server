# univention-management-console-module-diagnostic

UMC module for system diagnostics, running checks on DNS, certificates, connectors, services, and more.

## Binary packages

- `univention-management-console-module-diagnostic`

## Directory structure

- `umc/` -- UMC module (Python diagnostic plugins + JS frontend)
- `unittests/` -- Unit tests
- `debian/` -- Debian packaging
