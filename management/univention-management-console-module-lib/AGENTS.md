# univention-management-console-module-lib

UMC module providing low-level commands to control the UMC server (restart, software updates).

## Binary packages

- `univention-management-console-module-lib`

## Directory structure

- `umc/` -- UMC module (Python backend + JS frontend)
- `unittests/` -- Unit tests
- `debian/` -- Debian packaging
