[notifier/debug/level]
Description[de]=Diese Variable konfiguriert den Detailgrad der Logausgaben in /var/log/univention/notifier.log. Mögliche Werte: 0-4 (0: nur Fehlermeldungen bis 4: alle Debugausgaben).
Description[en]=This variable configures the verbosity of log messages in /var/log/univention/notifier.log. Possible values: 0-4 (0: only error messages to 4: all debug statements).
Type=int
Categories=service-ln

[notifier/autostart]
Description[de]=Die Variable konfiguriert den Startmodus des Univention Directory Notifier-Dienstes. Wird sie auf 'no' oder 'disabled' gesetzt, kann der Dienst nicht gestartet werden. Wird die auf 'manually' gesetzt, erfolgt kein automatischer Start zum Systemstart, der Dienst kann aber später gezielt gestartet werden.
Description[en]=This variable configures the start mode of the Univention Directory Notifier service. If set to 'no' or 'disabled', the service cannot be started. If the variable is set to 'manually', the service isn't started during system boot, but can be enabled manually at a later point.
Type=str
Categories=service-ln
