# univention-server-overview
Univention Server Overview - Web interface for overview over all server in a UCS domain

## univention-server-overview
Univention Server Overview is a web based interface which gives a fine overview over all servers in a UCS domain.
