# univention-server-overview

Web interface providing an overview of all servers in a UCS domain.

## Binary packages

- `univention-server-overview`

## Directory structure

- `umc/` -- UMC module (Python backend + JS frontend)
- `www/` -- Static web assets
- `conffiles/` -- UCR templates
- `debian/` -- Debian packaging
