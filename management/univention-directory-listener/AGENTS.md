# univention-directory-listener

Debian source package: UCS Directory Listener. Calls Python handlers upon LDAP changes, keeps local cache.

## Key contents
- `setup.py` for building the package
- Listener daemon that monitors LDAP for changes
- Python handler API for reacting to LDAP modifications
- Local cache management for LDAP state
