# univention-ldap-overlay-memberof

Configuration for the OpenLDAP memberOf overlay module, which automatically maintains reverse group membership attributes on user entries.

## Binary packages

- `univention-ldap-overlay-memberof`

## Directory structure

- `univention-update-memberof` -- Script to update memberOf attributes
- `conffiles/` -- UCR templates
- `debian/` -- Debian packaging
