[project]
name = "univention-directory-manager-modules"
version = "1.3.0"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["modules"]
include = ["univention.admin*", "univention.udm*"]
