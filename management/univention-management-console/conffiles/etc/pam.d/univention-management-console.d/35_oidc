@!@
if any(iss for key, iss in configRegistry.items() if key.startswith('ldap/server/sasl/oauthbearer/trusted-issuer/')):
    print('\nauth     sufficient        pam_oauthbearer.so config=/etc/ldap/sasl2/slapd.conf')
@!@
