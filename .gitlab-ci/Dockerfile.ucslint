ARG ucs=latest
FROM docker-registry.knut.univention.de/phahn/ucs-minbase:$ucs
ENV LANG C.UTF-8
RUN apt-get -qq update && apt-get -qq install --no-install-recommends devscripts python-flake8 python3-apt python3-debian python3-flake8 && apt-get clean
COPY . /usr/local/bin/
CMD ["ucslint"]
