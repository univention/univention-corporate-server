@!@
ucrlist = [(k, v) for k, v in configRegistry.items() if k.startswith('mail/postfix/smtpd_milters/')]
ucrlist.sort()
milters = [v for k, v in ucrlist]
if milters:
	print('smtpd_milters = {}'.format(' '.join(milters)))
@!@
