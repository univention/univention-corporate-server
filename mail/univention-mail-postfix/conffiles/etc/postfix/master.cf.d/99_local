@!@
import os

try:
	if os.stat('/etc/postfix/master.cf.local').st_size > 0:
		print('# --- master.cf.local ---')
		for line in open('/etc/postfix/master.cf.local', 'rb'):
			print(line.rstrip())
except (IOError, OSError) as exc:
	if exc.errno == 2:
		# No such file or directory
		pass
	else:
		print('# ERROR: {}'.format(exc))
@!@
