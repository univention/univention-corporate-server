#!/usr/bin/env sh

bug=$1

if test -z "$bug"; then
	bug=$(git log -n1 --format="%s" | sed -n "s/.*#\([0-9]*\).*/\1/p")
	echo "Guessed  bug number: $bug\n"
fi

echo "git commits (sorted from most recent to oldest):"
git log --grep "$bug" --format="%h %s"
