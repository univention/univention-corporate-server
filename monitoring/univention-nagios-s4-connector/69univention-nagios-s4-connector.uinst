#!/bin/sh
#
# Univention Nagios s4 Connector
#  unjoin script
#
# SPDX-FileCopyrightText: 2015-2026 Univention GmbH
# SPDX-License-Identifier: AGPL-3.0-only

## joinscript api: bindpwdfile

VERSION=1

. /usr/share/univention-join/joinscripthelper.lib
joinscript_init


eval "$(ucr shell)"

if [ ! "$nagios_client_autoregister" = "no" ] ; then
	univention-directory-manager nagios/service modify "$@" --ignore_exists \
			--dn "cn=UNIVENTION_S4CONNECTOR,cn=nagios,$ldap_base" \
			--remove assignedHosts="$ldap_hostdn"
fi

joinscript_remove_script_from_status_file univention-nagios-s4-connector

exit 0

