# univention-nagios-ad-connector

Transitional dummy package. AD connector monitoring functionality has been moved to `univention-monitoring-ad-connector` (built from `univention-monitoring-client`).

## Binary Packages

- `univention-nagios-ad-connector` -- Transitional package (empty).

## Directory Structure

- `debian/` -- Debian packaging files only.
