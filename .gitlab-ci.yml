stages:
    - build

build:
    stage: build
    tags:
        - ucs-build
    only:
        - /^[1-9]\.[0-9]-[0-9]$/    # Only build packages automatically for the main UCS branches
    script:
        - ./.build_unbuilt_packages
