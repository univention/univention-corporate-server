# Warning: This file is auto-generated and might be overwritten by 
#          univention-baseconfig.
#          Please edit the following file instead:
# Warnung: Diese Datei wurde automatisch generiert und kann durch 
#          univention-baseconfig überschrieben werden. 
#          Bitte bearbeiten Sie an Stelle dessen die folgende Datei:
# 
# 	/etc/univention/templates/files/etc/univention/client-boot-pxe/pxeconfig
# 


PROMPT 0
DEFAULT linux
IPAPPEND 1
@!@
line = 'root=/dev/nfs '
line += 'nfsroot=%s:/var/lib/univention-client-root ' % baseConfig['pxe/nfsroot']
line += 'DNSSERVER=%s ' % baseConfig['pxe/nameserver']
line += 'vt.default_utf8=0 '
if baseConfig.get('pxe/vga'):
	line += 'vga=%s ' % baseConfig['pxe/vga']
if baseConfig.get('pxe/initrd', "True").lower() in ['yes', 'true', '1']:
	line += 'splash=silent initrd=initrd.splash '
if baseConfig.get('pxe/quiet', "False").lower() in ['yes', 'true', '1']:
	line += 'quiet '
if 'pxe/ldapserver' in baseConfig.keys():
	line += 'ldapServer=%s ' % baseConfig['pxe/ldapserver']
if 'pxe/ldapport' in baseConfig.keys():
	line += 'ldapPort=%s ' % baseConfig['pxe/ldapport']
if 'pxe/append' in baseConfig.keys():
	line += '%s ' % baseConfig['pxe/append']
if baseConfig.get('pxe/loglevel', False):
	line += 'loglevel=%s ' % baseConfig['pxe/loglevel']

print 'APPEND %s' % line
@!@
LABEL linux
  KERNEL linux
