# Extended Network Documentation

Extended documentation for UCS IP and network management. Covers advanced proxy configuration, VLAN setup, and network-related topics.

## Format

Sphinx RST (`index.rst`, `conf.py`). Single-file document.

## Build

```sh
make html      # Build HTML
make spelling  # Spell check
```

Uses the Sphinx Docker image from the parent `doc/Makefile`.
