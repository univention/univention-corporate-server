# ext-networks

Extended IP and network management documentation (proxy configuration).

## Key contents
- `index.rst` -- Documentation entry point
- `conf.py`, `Makefile` -- Sphinx build configuration
