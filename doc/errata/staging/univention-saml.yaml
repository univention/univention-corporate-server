product: ucs
release: "5.0"
version: [1]
scope: ucs_5.0-0-errata5.0-1
src: univention-saml
fix: 
desc: |
 This update addresses the following issue(s):
 * The cookie attributes `Secure` and `SameSite` can now be set for the
   session and language cookies of SAML Identity Providers via UCR variables
   `saml/idp/session-cookie/secure`, `saml/idp/session-cookie/samesite`,
   `saml/idp/language-cookie/secure` and `saml/idp/language-cookie/samesite`.
bug: [54483]
