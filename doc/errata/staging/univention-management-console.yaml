product: ucs
release: "5.0"
version: [8]
scope: ucs_5.0-0-errata5.0-8
src: univention-management-console
fix: 12.0.34-5
desc: |
 This update addresses the following issues:
 * Ensure that `/usr/share/univention-management-console/oidc/oidc.json`
   has file permission `600`.
 * A new endpoint has been added to the UMC, supporting the refresh of
   all browser tabs with the Portal open when a user logs out.
 * Added `oidc-id-token` hint to UMC logout to disable Keycloak's logout
   confirmation dialog.
bug: [57505, 57467, 57475]
