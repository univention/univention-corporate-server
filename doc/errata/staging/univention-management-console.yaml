product: ucs
release: "5.0"
version: [9]
scope: ucs_5.0-0-errata5.0-9
src: univention-management-console
fix: 12.0.35-3
desc: |
 This update addresses the following issues:
 * Delete UMC session when OIDC token can not be refreshed after OP session
   deleted.
 * A package dependency to the python library `psycopg2` has been added.
 * The automatic browser reload of the `univention-portal` led to a visual
   logout every 5 minutes, since the initial assertion was expired then.
bug: [57515,57622,57563]
