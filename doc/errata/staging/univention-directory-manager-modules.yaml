product: ucs
release: "5.2"
version: [6]
scope: ucs_5.2-0-errata5.2-6
src: univention-directory-manager-modules
fix: 17.6.4
desc: |
 This update addresses the following issues:
 * The performance of object processing has been improved by reducing the
   overhead of mapping LDAP attributes to UDM properties. In environments with
   many objects, such as when retrieving large result sets through the UDM
   REST API, this noticeably speeds up object retrieval.
 * The UDM module search API has been extended to allow passing `opened=True`
   for UDM modules which support it.
bug: [59578, 59579]
