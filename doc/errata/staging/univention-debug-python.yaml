product: ucs
release: "5.0"
version: [6]
scope: ucs_5.0-0-errata5.0-6
src: univention-debug-python
fix: 12.0.3-4
desc: |
 This update addresses the following issues:
 * A new python module `univention.logging` has been introduced which provides
   a `python-logging` handler for `univention.debug`. It allows software
   components to use the `logging` interface of Python while logging into a
   `univention.debug` stream.
bug: [55324]
