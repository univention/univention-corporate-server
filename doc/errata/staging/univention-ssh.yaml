product: ucs
release: "5.0"
version: [1]
scope: ucs_5.0-0-errata5.0-1
src: univention-ssh
fix:
desc: |
 This update addresses the following issues:
 * Update the `univention-scp --help` and `univention-rsync` message to specify
   that the `--no-split` option must be set before the password file parameter
 * Fix spelling mistake in doc/univention-ssh.8: rsyn{v → c}
 * Improve message consistency between the man page and the `--help` messages
bug: [54588]
