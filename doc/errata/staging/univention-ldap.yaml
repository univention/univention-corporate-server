product: ucs
release: "5.0"
version: [5]
scope: ucs_5.0-0-errata5.0-5
src: univention-ldap
fix: 16.0.13-8
desc: |
 This update addresses the following issues:
 * The registration of LDAP schema files failed if the schema file is the
   first file in the directory and there is already a local schema file with
   the same name which was not registered via LDAP.
bug: [56857]
