product: ucs
release: "5.0"
version: [4]
scope: ucs_5.0-0-errata5.0-4
src: univention-monitoring-client
fix: 1.0.2-6
desc: |
 This update addresses the following issues:
 * A metric in `check_univention_ad_connector` was incorrectly written without
   the corresponding `connector` label as well as that metrics for an invalid
   `connector` were written.
bug: [56350]
