product: ucs
release: "4.0"
scope: ucs_4.0-0-errata4.0-2
src: univention-system-setup
fix: 
version: [2]
desc: |
  This update addresses the following issues:
  * Proxy settings were accidentally removed when changing network settings.
  * The netmask is not overwritten in the initial UCS configuration anymore if
    if was set manually.
  * A typo in the german translation has been corrected.
  * If accessible, the nameserver specified via DHCP is pre-configured on the
    network settings page.
  * The links on the last page of the setup wizard have been adjusted.
bug: [38593, 38593, 38215, 38330, 38850]
