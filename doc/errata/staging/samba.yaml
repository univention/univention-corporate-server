product: ucs
release: "4.4"
version: [5, 6]
scope: ucs_4.4-0-errata4.4-6
src: samba
fix: 2:4.10.18-1A~4.4.0.202010271532
desc: |
 This update addresses the following issues:
 * With Samba 4.10.18 a mitigation for Zerologin is available. "Secure
   Channel" (`Schannel`) can be deactivated for individual hosts, but must
   not to be deactivated globally. An example `smb.conf` looks like this:
     server schannel = yes
     server require schannel:host1$ = no
     server require schannel:host2$ = no
 * tdb
bug: [52130,52233]
cve:
- CVE-2020-1472
