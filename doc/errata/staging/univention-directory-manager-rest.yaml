product: ucs
release: "5.0"
version: [3]
scope: ucs_5.0-0-errata5.0-3
src: univention-directory-manager-rest
fix: 
desc: |
 This update addresses the following issues:
 * The HTTP status code of move operations (via PUT) has been fixed:
   The staus code is now `202 Accepted` instead of `201 Created`.
bug: [54623]
