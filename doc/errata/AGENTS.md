# errata

Errata update definitions as structured YAML files with JSON schema.

## Key contents
- `erratum.schema.json` -- JSON schema defining the errata YAML format
- `staging/` -- Advisories for planned package updates
- `published/` -- Advisories for released package updates (may not exist on all branches)
