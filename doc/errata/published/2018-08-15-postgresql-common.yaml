product: ucs
release: "4.3"
version: [1]
scope: ucs_4.3-0-errata4.3-1
src: postgresql-common
fix: 181+deb9u2A~4.3.1.201808081329
desc: |
 This update addresses the following issue:
 * Use 'deb-systemd-invoke stop "postgresql@$ver-*"' to prevent
   upgrading/removing server packages from stopping other major version
   clusters when running systemd. (Use deb-systemd-invoke instead of
   invoke-rc.d; jessie's invoke-rc.d does not support service patterns.)
bug: [47511]
erratum: 191
announced: 2018-08-15
