product: ucs
release: "5.0"
version: [6]
scope: ucs_5.0-0-errata5.0-6
src: univention-directory-manager-modules
fix: 15.0.25-5
desc: |
 This update addresses the following issues:
 * The log messages of UDM are now logged via the Python `logging` interface,
   which is configured to still log to the `univention.debug` log stream.
   This is a prerequisite for prefixing log lines with the request ID in the
   UDM REST API.
bug: [56970]
erratum: 957
announced: 2024-02-14
support: core
