product: ucs
release: "5.2"
version: [3]
scope: ucs_5.2-0-errata5.2-3
src: univention-ldap
fix: 18.3.0
desc: |
 This update addresses the following issues:
 * New LDAP schema, ACL's for federated account object type and
   new UCR variables `ldap/authz-regexp/users` (default `true`)
   and `ldap/authz-regexp/federated-accounts` (default `false`)
   for the configuration of the LDAP servers DN mapping for
   federated accounts.
bug: [58652]
erratum: 246
announced: 2025-09-30
support: core
