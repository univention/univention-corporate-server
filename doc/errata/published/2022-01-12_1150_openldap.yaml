product: ucs
release: "4.4"
version: [8]
scope: ucs_4.4-0-errata4.4-8
src: openldap
fix: 2.4.45+dfsg-1~bpo9+1A~4.4.0.202112020939
desc: |
 This update addresses the following issue:
 * Check `listener/listener` file if getting last_id fails.
 * Consistently write `<TransID>` if `last_id` could not be determined as all.
 * Log error if `listener/listener` file cannot be written.
 * Update `last_id` file atomically.
bug: [53821]
erratum: 1150
announced: 2022-01-12
