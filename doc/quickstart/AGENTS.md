# quickstart

Quickstart guide for UCS (with German translations).

## Key contents
- `index.rst` -- Documentation entry point
- `locales/` -- German translations
- `conf.py`, `Makefile` -- Sphinx build configuration
