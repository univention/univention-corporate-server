#!/bin/sh

file=/lib/univention-installer/modules/filesystem

counter=8
for fs in ext4 btrfs; do
	modinfo $fs >/dev/null 2>/dev/null
	if [ $? -eq 0 ]; then
		echo "$counter $fs" >> $file
		counter=$((counter +1))
	fi
done
