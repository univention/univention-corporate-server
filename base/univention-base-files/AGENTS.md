# univention-base-files

Debian source package: Base packages. Metapackage that depends on all required packages and recommends several others for a default UCS installation. Also contains base configuration files (moved from univention-config-registry).

## Key contents
- `debian/` -- Debian packaging files
- UCR templates and configuration files
