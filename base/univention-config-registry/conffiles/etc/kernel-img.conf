# Warning: This file is auto-generated and might be overwritten by
#          univention-config-registry.
#          Please edit the following file instead:
# Warnung: Diese Datei wurde automatisch generiert und kann durch
#          univention-config-registry überschrieben werden
#          Bitte bearbeiten Sie an Stelle dessen die folgende Datei:
#
#       /etc/univention/templates/files/etc/kernel-img.conf
#

@!@

print 'do_initrd = %s' % configRegistry.get('kernel/do_initrd', 'yes')

print 'do_bootfloppy = %s' % configRegistry.get('kernel/do_bootfloppy', 'no')

print 'silent_loader = %s' % configRegistry.get('kernel/silent_loader', 'yes')

print 'clobber_modules = %s' % configRegistry.get('kernel/clobber_modules', 'yes')

import os
arch=os.uname()[4]
if arch == "powerpc":
	print 'ramdisk = %s' % configRegistry.get('kernel/ramdisk', 'mkinitramfs mkinitrd.yaird')
if arch == "ppc64":
	print 'ramdisk = %s' % configRegistry.get('kernel/ramdisk', 'mkinitramfs mkinitrd.yaird')

print 'postinst_hook = %s' % configRegistry.get('kernel/postinst_hook', '/usr/sbin/update-grub')

@!@
