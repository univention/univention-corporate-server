# Warning: This file is auto-generated and might be overwritten by
#          univention-config-registry.
#          Please edit the following file instead:
# Warnung: Diese Datei wurde automatisch generiert und kann durch
#          univention-config-registry ueberschrieben werden.
#          Bitte bearbeiten Sie an Stelle dessen die folgende Datei:
#
# 	/etc/univention/templates/files/etc/hosts.d/00-base
#

127.0.0.1	localhost

@!@
for i in range(0,4):
	if configRegistry['interfaces/eth%s/address' % i]:
		name = configRegistry.get('interfaces/eth%s/hosts' % (i), '%s.%s %s' % (configRegistry['hostname'],configRegistry['domainname'],configRegistry['hostname']))
		if name:
			print '%s\t%s' % (configRegistry['interfaces/eth%s/address' % i],name)

for i in range(0,4):
	for j in range(0,4):
		if configRegistry['interfaces/eth%s_%s/address' % (i,j)]:
			name = configRegistry.get('interfaces/eth%s_%s/hosts' % (i,j), '%s.%s %s' % (configRegistry['hostname'],configRegistry['domainname'],configRegistry['hostname']))
			if name:
				print '%s\t%s' % (configRegistry['interfaces/eth%s_%s/address' % (i,j)],name)

@!@
