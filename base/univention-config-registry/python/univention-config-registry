#!/usr/bin/python3
#
# Univention Configuration Registry
#  start script
#
# SPDX-FileCopyrightText: 2004-2026 Univention GmbH
# SPDX-License-Identifier: AGPL-3.0-only

import os
import sys

import univention.config_registry as ub


if __name__ == '__main__' and not os.path.exists('/etc/univention/disable_config_registry'):
    try:
        sys.exit(ub.main(sys.argv[1:]))
    except ub.StrictModeException as e:
        print('Error: UCR is running in strict mode and thus cannot accept the given input:')
        print(e)
        sys.exit(1)
