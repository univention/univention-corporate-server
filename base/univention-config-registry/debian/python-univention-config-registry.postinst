#!/bin/sh
# preinst script for python-univention-config-registry

set -e

if [ "$1" = upgrade ] && dpkg --compare-versions "$2" lt 7.0.0; then
       pycentral pkgremove python-univention-config-registry
fi

#DEBHELPER#

exit 0
