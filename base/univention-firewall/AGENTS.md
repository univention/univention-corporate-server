# univention-firewall

Integrates iptables firewall management with Univention Config Registry (UCR). Firewall rules are configured via UCR variables.

## Binary Packages

- `univention-firewall`

## Key Directories

- `conffiles/` -- UCR templates generating iptables rules
- `etc/` -- default configuration files
