#!/bin/sh

@%@BCWARNING=# @%@

@!@
if not baseConfig.is_false('security/services/localhost'):
	print '# allow loopback traffic'
	print '/sbin/iptables -A INPUT -i lo -j ACCEPT'
	print '/sbin/iptables -A OUTPUT -o lo -j ACCEPT'
@!@
