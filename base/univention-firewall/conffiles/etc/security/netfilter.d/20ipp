#!/bin/sh

@%@BCWARNING=# @%@

@!@
if baseConfig.has_key('security/services/ipp') and baseConfig['security/services/ipp'] == 'disabled':
	print '/sbin/iptables -A INPUT  -p tcp --dport 631 -j REJECT'
	print '/sbin/iptables -A INPUT  -p udp --dport 631 -j REJECT'
@!@
