# univention-maintenance-mode

Debian source package: Minimal web server for maintenance mode during release updates. Serves a maintenance page while the system is being upgraded.

## Key contents
- `debian/` -- Debian packaging files
- Lightweight web server and maintenance page templates
