@%@UCRWARNING=# @%@

PATH=/usr/sbin:/usr/bin:/sbin:/bin

@!@
if configRegistry.get('server/role') == 'domaincontroller_master':
	interval = configRegistry.get('directory/manager/user/lock_expired_accounts/cron/entry', '17 3 * * *')
	disabled = '# '
	if configRegistry.is_true('directory/manager/user/lock_expired_accounts/cron/enabled', True):
		disabled = ''
	print '%s%s   root   /usr/share/univention-directory-manager-tools/lock_expired_accounts --only-last-week --quiet' % (disabled, interval)
@!@
