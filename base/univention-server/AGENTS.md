# univention-server

Debian source package: Primary Directory Node installation package. Sets up the core UCS server role.

## Key contents
- `debian/` -- Debian packaging files
- Configuration and dependencies for UCS server roles
