#!/bin/sh
#DEBHELPER#

eval "$(ucr shell locale)"
new_locale="zh_TW.UTF-8:UTF-8"
case "${locale}" in
    *"${new_locale}"*) echo "Locale ${new_locale} already known" ;;
    *)	ucr set locale="${locale} ${new_locale}" ;;
esac

ucr set ucs/server/languages/zh_TW?"Traditional Chinese"

exit 0