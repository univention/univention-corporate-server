# univention-licence-python

Debian source package: Licence validation library for Python 3. Provides Python bindings for the univention-licence C library.

Contains Python code: `setup.py` builds and installs the Python interface.

## Key contents
- `debian/` -- Debian packaging files
- `setup.py` -- Python package build configuration
- Python C extension wrapping the licence validation library
