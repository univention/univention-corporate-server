# univention-sudo

Debian source package: Sudo rules. Provides default rules for the sudo command on UCS systems.

## Key contents
- `debian/` -- Debian packaging files
- UCR templates for sudoers configuration
