# univention-python

Common Python 3 modules used across UCS (`python3-univention`). Provides base LDAP, password, config, and debugging integration.

## Binary Packages

- `python3-univention`

## Key Directories

- `modules/` -- Python module source (univention base package)
- `unittests/` -- unit tests
- `setup.py` -- setuptools configuration
