# univention-initrd

Scripts for the initial ramdisk (initramfs) used during UCS boot.

## Binary Packages

- `univention-initrd`

## Key Directories

- `conffiles/` -- UCR templates for initramfs configuration
