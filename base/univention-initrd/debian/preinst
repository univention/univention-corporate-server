#!/bin/sh

if [ "$1" = configure -a -n "$2" ] && dpkg --compare-versions "$2" lt 1.0.1-1; then
	modules=$(cat /etc/mkinitrd/modules  | sed -e 's|#.*||g' | grep [a-z])
	univention-baseconfig set mkinitrd/modules=$(echo $modules | sed -e 's| |;|g')
fi

#DEBHELPER#

exit 0
