# univention-group-membership-cache

Debian source package: Group cache. Holds a cache for user and group membership, including nested groups.

Contains Python code: provides the `univention.ldap_cache` Python module (no setup.py).

## Key contents
- `debian/` -- Debian packaging files
- `univention/ldap_cache/` -- Python module for group membership caching
