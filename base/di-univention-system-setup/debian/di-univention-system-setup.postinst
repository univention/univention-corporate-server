#!/bin/sh

. /usr/share/debconf/confmodule

# start univention-system-setup if in GTK mode
if [ -n "$DISPLAY" ]; then
	chroot /target /usr/share/univention-system-setup/startxwithfirefox_installer
fi

db_input critical di-univention-system-setup/foo
db_go

exit 0
