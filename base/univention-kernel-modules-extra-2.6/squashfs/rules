export CONFIG_SQUASHFS=m

$(BUILD_STAMP):
	$(MAKE) -C $(HEADERS_DIR) M=$(CURDIR)/$(DIR)/linux-2.6 $(JOBS_ARG)
	touch $@

install:
	$(MAKE) -C $(HEADERS_DIR) M=$(CURDIR)/$(DIR)/linux-2.6 modules_install INSTALL_MOD_PATH=$(PACKAGE_DIR) INSTALL_MOD_DIR=extra/$(MODULE)

