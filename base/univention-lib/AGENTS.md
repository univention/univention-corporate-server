# univention-lib

Debian source package: Common scripting functions for Python 3 scripts and shell scripts.

Contains Python code: provides the `univention.lib` Python modules.

## Key contents
- `debian/` -- Debian packaging files
- `univention/lib/` -- Python utility modules used across UCS
- Shell library functions
