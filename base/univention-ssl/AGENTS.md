# univention-ssl

Debian source package: SSL/TLS certificates. Creates root CA and service certificates for UCS.

## Key contents
- `debian/` -- Debian packaging files
- Scripts for CA and certificate management (creation, renewal, revocation)
