# Warning: This file is auto-generated and might be overwritten by
#          univention-baseconfig.
#          Please edit the following file instead:
# Warnung: Diese Datei wurde automatisch generiert und kann durch
#          univention-baseconfig überschrieben werden.
#          Bitte bearbeiten Sie an Stelle dessen die folgende Datei:
#
# 	/etc/univention/templates/files/etc/resolv.conf
#


@!@
domain=False

if baseConfig.has_key('domain/domainname'):
	print 'domain  %s' % baseConfig['domain/domainname']
	domain=True
if baseConfig.has_key('domain/search'):
	print 'search %s' % baseConfig['domain/search']
	domain=True

if not domain and baseConfig['domainname']:
	print 'domain	'+baseConfig['domainname']

if baseConfig['nameserver1']:
	print 'nameserver  '+baseConfig['nameserver1']
if baseConfig['nameserver2']:
	print 'nameserver  '+baseConfig['nameserver2']
if baseConfig['nameserver3']:
	print 'nameserver  '+baseConfig['nameserver3']

if baseConfig['nameserver/option/timeout']:
	print 'options timeout:%s' % baseConfig['nameserver/option/timeout']
@!@
