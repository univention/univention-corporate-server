# Warning: This file is auto-generated and might be overwritten by
#          univention-baseconfig.
#          Please edit the following file instead:
# Warnung: Diese Datei wurde automatisch generiert und kann durch
#          univention-baseconfig Überschrieben werden
#          Bitte bearbeiten Sie an Stelle dessen die folgende Datei:
#
#       /etc/univention/templates/files/etc/kernel-img.conf
#

@!@

print 'do_initrd = %s' % baseConfig.get('kernel/do_initrd', 'yes')

print 'do_bootfloppy = %s' % baseConfig.get('kernel/do_bootfloppy', 'no')

print 'silent_loader = %s' % baseConfig.get('kernel/silent_loader', 'yes')

import os
arch=os.uname()[4]
if arch == "powerpc":
	print 'ramdisk = %s' % baseConfig.get('kernel/ramdisk', 'mkinitramfs mkinitrd.yaird')
if arch == "ppc64":
	print 'ramdisk = %s' % baseConfig.get('kernel/ramdisk', 'mkinitramfs mkinitrd.yaird')

print 'postinst_hook = %s' % baseConfig.get('kernel/postinst_hook', '/sbin/update-grub')

@!@
