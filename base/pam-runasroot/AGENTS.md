# pam-runasroot

Debian source package: PAM module to execute scripts during authentication. Allows running scripts during the auth and session setup phase of PAM.

## Key contents
- `debian/` -- Debian packaging files
- C source for the PAM module
