# univention-apache

Debian source package: Apache2 configuration. Basic configuration for the Apache web server.

## Key contents
- `conffiles/` -- UCR template files for Apache configuration
- `univention-add-vhost` -- script to add virtual hosts
- `www/` -- static web content
