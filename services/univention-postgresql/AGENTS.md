# univention-postgresql

UCS-specific configuration for the PostgreSQL database server. Meta-package that pulls in the version-specific configuration.

## Binary Packages

- `univention-postgresql` -- meta-package selecting the correct PostgreSQL version
- `univention-postgresql-15` -- configuration for PostgreSQL 15

## Directory Structure

- `conffiles/` -- UCR templates for PostgreSQL configuration
