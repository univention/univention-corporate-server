
; -----------------------------------------------------------------------------------------------------------
@!@
import os
import sys

for includefilepath in ('/etc/samba/base.conf', '/etc/samba/installs.conf'):
	if os.path.exists(includefilepath):
		print("\tinclude = %s" % (includefilepath,))
@!@
