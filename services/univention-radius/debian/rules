#!/usr/bin/make -f
#
# Univention RADIUS 802.1X
#  rules file for the debian package
#
# SPDX-FileCopyrightText: 2011-2026 Univention GmbH
# SPDX-License-Identifier: AGPL-3.0-only

export PYBUILD_NAME=univention-radius
export PYBUILD_AFTER_INSTALL=$(RM) '{destdir}/{install_dir}/univention/__init__.py'

export DH_VERBOSE = 1
# read /usr/share/perl5/Debian/Debhelper/Buildsystem/cargo.pm and /usr/share/cargo/bin/cargo !
export CARGO_HOME=$(CURDIR)/debian/cargo_home
export PATH := /usr/share/cargo/bin:$(PATH)

include /usr/share/dpkg/architecture.mk
include /usr/share/dpkg/buildflags.mk
include /usr/share/rustc/architecture.mk
export CFLAGS CXXFLAGS CPPFLAGS LDFLAGS
export DEB_HOST_RUST_TYPE DEB_HOST_GNU_TYPE

%:
	dh $@ --with python3,ucr,cargo --buildsystem=pybuild

override_dh_auto_clean:
	sed -i '/elsif ($$package->{Architecture} ne '"'"'all'"'"')/a             next if $$package->{Package} eq "python3-univention-radius";' /usr/share/perl5/Debian/Debhelper/Buildsystem/cargo.pm
	dh_auto_clean
	dh_auto_clean --buildsystem cargo

override_dh_auto_configure:
	dh_auto_configure
	dh_auto_configure --buildsystem cargo

override_dh_auto_build:
	dh_auto_build
	dh_auto_build --buildsystem cargo

override_dh_auto_test-arch:
	dh_auto_test
	dh_auto_test --buildsystem cargo

override_dh_auto_install:
	dh_auto_install
	dh_auto_install --buildsystem cargo

override_dh_auto_test:
	dh_auto_test --buildsystem cargo
	cargo test
