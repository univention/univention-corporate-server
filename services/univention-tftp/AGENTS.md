# univention-tftp

Debian source package: TFTP server for network booting.

## Key contents
- `conffiles/` -- UCR template files for TFTP configuration
- `default` -- default service configuration
- `debian/` -- Debian packaging files
