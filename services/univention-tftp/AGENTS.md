# univention-tftp

Installs and configures a TFTP server (atftpd) for PXE network booting.

## Binary Packages

- `univention-tftp` -- TFTP server with PXE boot configuration

## Directory Structure

- `conffiles/` -- UCR templates for TFTP/PXE configuration
- `default` -- default configuration file
