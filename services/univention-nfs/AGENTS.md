# univention-nfs

Debian source package: NFS server. Shares managed with univention-share infrastructure.

## Key contents
- `nfs-shares.py` -- LDAP listener module for NFS share management
- `nfs-homes.py` -- LDAP listener module for NFS home directories
- `conffiles/` -- UCR template files for NFS configuration
