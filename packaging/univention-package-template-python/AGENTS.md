# univention-package-template-python

Template/scaffold for creating new UCS Python module packages. Demonstrates the standard structure for shipping a Python 3 library as a Debian package.

## Binary Packages

- `python3-univention-package-template` -- Example Python 3 module package

## Directory Structure

- `python/` -- Python source modules
