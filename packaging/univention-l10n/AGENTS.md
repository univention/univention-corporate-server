# univention-l10n

Debian source package: Development tools for localization of UCS.

## Key contents
- Installs the `univention-l10n` Python package (configured via `setup.cfg`)
- Provides tooling for extracting, compiling, and managing translations
- Supports localization workflows for UCS Debian packages
