# univention-directory-manager-module-example

Debian source package: UDM example module to manage IP phones. Demonstrates how custom LDAP objects can be managed with UDM.

## Key contents
- Example UDM module for a custom LDAP object type (IP phones)
- Reference implementation for writing new UDM modules
- Includes LDAP schema, UDM handler, and packaging boilerplate
