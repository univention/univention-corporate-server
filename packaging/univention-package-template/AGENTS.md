# univention-package-template

Debian source package: Package template. Example result of a univention package template.

## Key contents
- Reference output of the UCS package templating process
- Shows the expected directory structure and debian packaging files
- Useful as a baseline when creating new UCS packages
