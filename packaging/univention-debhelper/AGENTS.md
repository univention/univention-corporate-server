# univention-debhelper

Debian source package: Helper programs for debian/rules. Programs to automate common tasks related to building UCS packages.

## Key contents
- Provides debhelper extensions for UCS package builds
- Automates repetitive packaging tasks in `debian/rules`
- Uses `setup.py` for Python components
