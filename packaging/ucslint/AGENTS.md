# ucslint

Debian source package: Check packaging for UCS. Checks debian source packages against common packaging mistakes.

## Key contents
- Installs the `univention.ucslint` Python package (configured via `setup.cfg`)
- Provides lint checks for UCS Debian package sources
- Validates packaging conventions and flags common errors
