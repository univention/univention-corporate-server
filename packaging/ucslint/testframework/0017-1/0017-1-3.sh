eval `ucr shell`
eval `ucr shell foobar`

eval `univention-baseconfig shell`
eval `univention-baseconfig shell foobar`

eval `univention-config-registry shell`
eval `univention-config-registry shell foobar`


eval ` ucr shell `
eval ` ucr shell foobar `

eval ` univention-baseconfig shell `
eval ` univention-baseconfig shell foobar `

eval ` univention-config-registry shell `
eval ` univention-config-registry shell foobar `



eval $(ucr shell)
eval $(ucr shell foobar)

eval "$(ucr shell)"
eval "$(ucr shell foobar)"

eval "$(univention-baseconfig shell)"
eval "$(univention-baseconfig shell foobar)"

eval "$(univention-config-registry shell)"
eval "$(univention-config-registry shell foobar)"


eval "$( ucr shell )"
eval "$( ucr shell foobar )"

eval "$( univention-baseconfig shell )"
eval "$( univention-baseconfig shell foobar )"

eval "$( univention-config-registry shell )"
eval "$( univention-config-registry shell foobar )"



eval $(ucr shell)
eval $(ucr shell foobar)

eval $(univention-baseconfig shell)
eval $(univention-baseconfig shell foobar)

eval $(univention-config-registry shell)
eval $(univention-config-registry shell foobar)


eval $( ucr shell )
eval $( ucr shell foobar )

eval $( univention-baseconfig shell )
eval $( univention-baseconfig shell foobar )

eval $( univention-config-registry shell )
eval $( univention-config-registry shell foobar )

