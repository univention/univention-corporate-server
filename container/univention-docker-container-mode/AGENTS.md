# univention-docker-container-mode

Transitional (dummy) package. This package can be safely removed.

## Binary Packages

- `univention-docker-container-mode`

## Directory Structure

- `debian/` -- Debian packaging files (only content in this source package).
