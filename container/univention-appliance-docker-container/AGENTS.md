# univention-appliance-docker-container

Debian source package: UCS appliance settings for Docker container. Installs settings for running UCS appliance setup inside a Docker container.

## Key contents
- `debian/` - Debian packaging files
- `debian/univention-appliance-docker-container.postinst` - Post-installation script for configuring appliance settings
