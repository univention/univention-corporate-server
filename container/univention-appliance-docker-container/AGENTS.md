# univention-appliance-docker-container

Installs settings for running UCS appliance setup inside a Docker container. Depends on `univention-docker-container-mode` and `univention-system-setup-boot`.

## Binary Packages

- `univention-appliance-docker-container`

## Directory Structure

- `debian/` -- Debian packaging files (only content in this source package).
